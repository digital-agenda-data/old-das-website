  <div id="printcontainer">
      <h1>The country profile with all indicator groups</h1>
      Each graph and corresponding table is positioned on a different page creating a consistent print.
      <?php foreach ($printcontent as $p) { echo $p; }; ?>
  </div>
